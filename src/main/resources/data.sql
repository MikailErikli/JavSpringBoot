CREATE TABLE IF NOT EXISTS rayon(id INT PRIMARY KEY, theme VARCHAR(255) NOT NULL);
CREATE TABLE IF NOT EXISTS ouvrage(id INT PRIMARY KEY, title VARCHAR(255) NOT NULL, author VARCHAR(255) NOT NULL, isbn VARCHAR(255) NOT NULL, price FLOAT NOT NULL, stock INT DEFAULT 0, rayon INT NOT NULL);